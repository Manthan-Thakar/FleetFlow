rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write for now
    // Will be configured later with proper security rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
